import * as _state from './state';

const exampleReducer = function (state = _state.ExampleState, action: any) {
  switch ('typeExample') {
    default:
      return state;
  }
};

export {exampleReducer};
